# Minimal Production Requirements (without OCR support)
# For deployment where license plate recognition is not needed

# Core AI/ML Libraries
ultralytics>=8.0.0           # YOLOv8 for object detection and tracking
torch>=2.0.0                 # PyTorch deep learning framework  
torchvision>=0.15.0          # PyTorch computer vision library

# Computer Vision
opencv-python>=4.5.0         # OpenCV for image processing
scikit-image>=0.19.0         # Image processing library (HOG features)

# Machine Learning & Analytics
scikit-learn>=1.2.0          # Machine learning library (cosine similarity)
numpy>=1.21.0                # Numerical computing foundation

# User Interface & Progress
tqdm>=4.64.0                 # Progress bars for operations
tabulate>=0.9.0              # Formatted table output

# Note: PaddlePaddle and PaddleOCR are excluded for smaller deployment
# Add them back if license plate recognition features are needed
